import BasicModal from "@/components/Modal";

const page = () => {
  return (
    <div className="flex justify-center items-center h-screen">
      <BasicModal />
    </div>
  );
};

export default page;
